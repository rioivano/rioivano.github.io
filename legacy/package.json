{
  "name": "rio-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tailwindcss -i ./style.css -o ./output.css --watch",
    "build": "tailwindcss -i ./style.css -o ./output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
